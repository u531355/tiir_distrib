package fil.tiir.fakedistrib.service;
/*
import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import fil.tiir.fakedistrib.dao.BanqueDaoMock;

@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration //A tester, sinon => @ContextConfiguration(locations="contextDifferent.xml")
public class InteractionBanqueTest {
/*
    @Autowired
    private InteractionBanque interactionBanque;
    @Autowired
    @Qualifier(value="mock")
    private ClientDaoMock client;

    @Before
    public void initTest() {
        client.initMock();
    }

    @Test
    public void testConnecter() {
        // Arrange
        client.setHash() => Le password
        client.setNumeroCart("07619")
        final int expected = true;
        
        // Act
        interactionBanque.connecter(client);
        
        // Assert
        assertEquals(expected, client.isConnected());
    }

    @Test
    public void testCompterCrit2() {
        client.setHash() => Le password
        client.setNumeroCart("192830")
        final int expected = true;
        
        // Act
        interactionBanque.connecter(client);
        
        // Assert
        assertEquals(expected, client.isConnected());
    }
}*/